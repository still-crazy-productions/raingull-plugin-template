def platform_to_raingull(raw_message):
    # Translate raw incoming messages clearly to RainGull format
    return standardized_message

def raingull_to_platform(standardized_message):
    # Translate RainGull messages clearly to platform-specific outgoing format
    return platform_message